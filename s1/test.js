//const math=require('./math');
const {sum, mult}  = require('math');
//import {sum, mult}  from 'math';
let tab = [1,3,5];
console.log('for tab :'+tab+' the sum is : '+sum(tab));
//console.log(`for tab :${tab} the sum is : ${math.sum(tab)}`);
